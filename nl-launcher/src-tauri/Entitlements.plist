<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation (required for some frameworks) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow unsigned executable memory (required for Tauri) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Disable library validation (required for external binaries like adb, nl-host) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Allow dyld environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    
    <!-- Network access (client) -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Network access (server - for local connections) -->
    <key>com.apple.security.network.server</key>
    <true/>
    
    <!-- USB device access (for ADB) -->
    <key>com.apple.security.device.usb</key>
    <true/>
</dict>
</plist>
